// Development balance values. These are server-authoritative and intentionally
// isolated in one place so they can be tuned without changing route logic.
export const TERRITORY_CLAIM_COST = 5_000;
export const TERRITORY_LEASE_DAYS = 30;
export const TERRITORY_INTERACTION_DISTANCE_METERS = 50;
